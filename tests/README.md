# Unit Tests and Web Tests

make sure that `stest` and `stest-all` executables are in your `$PATH`
make sure shared `pest` and `phpunit` from `php-tools` are available too

If the test suite grows, split tests into topic-focused subdirectories under `tests/`.
Use `tests/fixtures-src/` for source files that exist only to support tests.
